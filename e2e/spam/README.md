# End-to-End Migration Spam Tool

This tool simply broadcasts zero values transactions on the specified legacy node.

```
./spam -nodeAPIURI="https://<legacy-node-api-uri>"
```

Hit ctrl+c to terminate the program.